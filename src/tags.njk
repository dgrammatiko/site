---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - blog
    - tagList
  addAllPagesToCollections: true
layout: blog.njk
renderData:
  title: Tagged “{{ tag }}”
  permalink: /tags/{{ tag }}/
---
<h1>Tagged “{{ tag }}”</h1>

{% set postslist = collections[ tag ] %}
{% include "listing-all.njk" %}

<p>See <a href="{{ '/tags/' | url }}">all tags</a>.</p>
